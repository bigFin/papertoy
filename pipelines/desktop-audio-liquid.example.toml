[[passes]]
kind = base
path = "${PAPERTOY_DEFAULT_SHADER}"

[[passes]]
kind = postprocess
effect = pulse_zoom
strength = 0.45

[[passes]]
kind = postprocess
path = "audio-liquid-orbit.post.glsl"
strength = 0.90

[[passes]]
kind = postprocess
effect = glow_grade
strength = 0.65

[audio]
enabled = true
capture = sink
target = "auto"

[modulation]
time_reactive = true
time_strength = 1.35
visual_reactive = false
visual_strength = 0.70
visual_style = drift
