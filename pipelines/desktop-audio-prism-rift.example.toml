[[passes]]
kind = base
path = "${PAPERTOY_DEFAULT_SHADER}"

[[passes]]
kind = postprocess
effect = shock_ring
strength = 0.55

[[passes]]
kind = postprocess
path = "audio-prism-rift.post.glsl"
strength = 0.95

[[passes]]
kind = postprocess
effect = impact_flash
strength = 0.70

[audio]
enabled = true
capture = sink
target = "auto"

[modulation]
time_reactive = true
time_strength = 1.85
visual_reactive = false
visual_strength = 0.90
visual_style = heat
